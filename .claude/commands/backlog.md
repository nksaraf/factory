Review the current conversation for any ideas, features, TODOs, or deferred work that have been discussed but NOT yet implemented.

Then read `BACKLOG.md` at the repo root to understand what's already tracked.

For each untracked item you find in the conversation:
1. Determine which section of BACKLOG.md it belongs to (or create a new section if needed)
2. Add it as a `[ ]` checkbox item with a brief, concrete description
3. If it relates to an existing item, nest it as a sub-bullet or add detail

Rules:
- Do NOT add items that are already in BACKLOG.md
- Do NOT mark existing items as done — only add new ones
- Do NOT remove or reorder existing items
- Keep descriptions concise and actionable (what, not why)
- If an idea is vague, sharpen it into something concrete before adding
- Group related items together under the most relevant section

After updating, show the user a summary of what was added.
